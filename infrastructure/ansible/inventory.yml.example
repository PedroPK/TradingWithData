all:
  children:
    trading_with_data:
      hosts:
        vps:
          ansible_host: 203.0.113.10
          ansible_user: deploy
          ansible_ssh_private_key_file: ~/.ssh/trading-with-data
      vars:
        app_domain: dados.example.com
        app_repository: https://github.com/PedroPK/TradingWithData.git
        app_version: main
